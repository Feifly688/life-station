# 翡栖视觉规范（Design Spec v1）

> 本文是**唯一视觉基准**。任何新增页面/组件都应从这里取令牌（`ui/theme/Tokens.kt`），
> 不要在业务代码里写裸数值（`16.dp`、`0xFFxxxxxx`）。

---

## 0. 设计原则

| 原则 | 含义 | 落地要求 |
| --- | --- | --- |
| **暖而不腻** | 主色为紫棕，辅以沙色/苔绿，底为米白，整体像纸张与陶土 | 大面积留白用 `Background`，强调只用 `Primary`，语义色（涨跌/过期）只用于状态 |
| **层次靠光影，不靠描边** | 层级差异用阴影+底色深浅表达，避免到处加边框 | 卡片默认无描边；只有浮层/玻璃面用 1dp 高光边 |
| **动静有度** | 只对"状态变化"做动效（展开、拖动、切换），不做装饰性动画 | 时长 120/200/320ms 三档，见 §5 |
| **一屏一重点** | 每个页面只有一个"主视觉"，其余退为背景 | 见 §7 各页侧重 |

---

## 1. 颜色令牌

已有暖色调色板（`ui/theme/Color.kt`）继续沿用，**新增/明确**以下语义：

| 令牌 | 值 | 用途 | 禁止 |
| --- | --- | --- | --- |
| `Primary` | `#6B4F5B` 紫棕 | 主按钮、选中态、强调数字 | 不做大面积填充背景 |
| `Secondary` | `#B08E6C` 沙 | 次级强调、图标点缀 | — |
| `Tertiary` | `#8FA277` 苔绿 | 成功/健康类信息 | 不用于警告 |
| `Background` | `#FFFBF5` 米白 | 页面底 | 不用于卡片（卡片用 `Surface`） |
| `Surface` | `#FFFFFF` | 卡片/浮层底 | 纯白面积不超过一屏 60% |
| `SurfaceVariant` | `#F6EEE4` | 分区底、输入框底 | — |
| `OnSurface` | `#453C34` | 正文 | — |
| `OnSurfaceVariant` | `#8A7C6C` | 次要文字/单位 | **不用于正文**（对比度不足） |
| `Outline` / `OutlineVariant` | `#E6DACA` / `#F2E9DD` | 分隔线、输入框边 | 卡片默认不加边 |
| `ExpenseRed` | `#D9694F` | 支出、**已过期**、危险操作 | 不用于普通强调 |
| `IncomeGreen` | `#6E9B62` | 收入 | — |
| `WarmAccent` | `#E9A63C` | 提醒、待办类强调 | — |
| `CardRed` / `CardGreen` | `#FDEDE5` / `#EDF4E6` | 统计卡底色 | 只用于统计卡，不做页面背景 |

**规则**：
1. 正文对比度 ≥ 4.5:1（`OnSurface` on `Surface` ✓）；次要信息可降到 3:1（`OnSurfaceVariant`）。
2. 同一屏内**语义色不超过 2 种**（例如"支出红 + 待办黄"可以，"红绿黄同时出现"不行）。
3. 数字与金额一律用 `titleMedium`/`titleLarge` + `Primary`，单位与说明用小号 `OnSurfaceVariant`。

---

## 2. 字体

沿用 `Type.kt`（系统默认字体，保证中文显示完整），明确使用场景：

| 样式 | 字号/行高 | 用途 | 字重 |
| --- | --- | --- | --- |
| `headlineLarge` | 28/36 | 页面主标题（首页问候下方标语） | SemiBold |
| `titleLarge` | 22/28 | 区块大标题、金额主数值 | SemiBold |
| `titleMedium` | 16/24 | 卡片标题、列表主文本 | Medium |
| `bodyLarge` | 16/24 | 长文本、输入框内容 | Normal |
| `bodyMedium` | 14/20 | 列表副文本、正文 | Normal |
| `labelLarge` | 14/20 | 按钮文字、Tab 标签 | Medium |
| `labelMedium` | 12/16 | 时间、单位、次级标签 | Medium |
| `labelSmall` | 11/16 | 状态标签（已过期/循环）、角标 | Medium |

**规则**：
- 一段文字只用一个样式，**不靠字号差做层级**，靠颜色与间距；
- 中文**不加字间距**，数字/英文标签可加 `0.2sp`；
- 卡片标题最多 1 行、正文最多 2 行，超出省略（`maxLines` + `Ellipsis`），避免高度抖动。

---

## 3. 间距与圆角

**间距**（`FeiQiSpacing`，4 的倍数）：

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `xs` | 4 | 图标与文字 |
| `sm` | 8 | 同组元素之间 |
| `md` | 12 | 卡片内部行距 |
| `lg` | 16 | **页面左右边距**、卡片内边距 |
| `xl` | 24 | 区块之间 |
| `xxl` | 32 | 页面首屏留白、空态上下留白 |

**圆角**（`FeiQiRadius`）：

| 令牌 | 值 | 用途 |
| --- | --- | --- |
| `sm` | 12 | 输入框、Chip、小按钮 |
| `md` | 16 | 列表项、次级卡片 |
| `lg` | 20 | **主卡片、统计卡** |
| `xl` | 28 | 弹窗、浮层面板、大卡片 |
| `pill` | 100% | 主按钮、Tab 指示 |

**规则**：同屏同类元素圆角必须一致；嵌套圆角 = 外层 - 内边距（避免"角套角"）。

---

## 4. 液态玻璃（Liquid Glass）

**适用**（浮在内容之上的面）：底部导航、弹窗/确认框、悬浮面板、区块拖拽手柄容器、语录卡等"海报式"卡片。
**不适用**：列表项、输入框、正文卡片——玻璃会削弱可读性并拖慢滚动。

### 4.1 结构（三层叠加）

| 层 | 规格 | 作用 |
| --- | --- | --- |
| **填充** | **暖调基色** = `Surface` 与 `Secondary` 按 12% 混合，再按 55% / 68% / 82% 三档透明（`Thin` / `Regular` / `Thick`）；对角渐变（左上更亮更实 → 右下更暖更透） | 半透明磨砂质感 + 厚度差 |
| **斜向高光** | 白 26% / 20% / 15% 的对角反光带（sheen），向左下淡出 | 定向反射，**让玻璃在浅色底上也能被看见** |
| **双色调边** | 1dp：左上白 55–68% 高光 + 右下 `OnSurface` 7% 暗边 | 玻璃"断面"，比单色描边立体 |
| **光影** | 暖色阴影（`OnSurface` 16–22% alpha），模糊 12–24dp | 悬浮感 |

> ⚠️ **踩过的坑（v1.7.1 修正）**：最初用"纯白 + 半透明"，而页面底是米白 `#FFFBF5` ——
> 白叠白明度几乎相同，**效果肉眼不可见**。所以基色必须带暖调、并靠 sheen 与双色调边提供可辨差异。
> 另一个事实：底部导航栏所在区域**背后没有内容滚动**（Scaffold 把内容排在栏上方），
> 因此它的玻璃感主要来自"暖调 + 顶边 + 抬升阴影"，而不是"透出内容"。
> 若要真正的"内容在栏下滚动"，需把导航栏改为覆盖层并同步加大各页底部内边距（下一步可选优化）。

### 4.2 关于"背景模糊"

Compose **原生没有背景模糊（backdrop blur）**：`Modifier.blur` 模糊的是自身内容，不是身后的画面。
因此规范采用"半透明填充 + 渐变 + 高光边"实现**视觉上的磨砂玻璃**（在所有 API 上一致，minSdk 24 也能用）；
若以后要真·背景模糊，需引入 `haze` 之类的三方库（届时只需替换 `GlassSurface` 的实现，调用点不用改）。

### 4.3 可读性兜底

- 玻璃面上的文字**必须**用 `OnSurface`（深棕）而非纯黑/纯白；
- 玻璃面叠加在图像/彩色内容上时，自动升档（`Thin` → `Regular`）；
- **状态色不玻璃化**：已过期、警告等语义标签保持实色，避免被背景干扰。

---

## 5. 交互状态与动效

| 状态 | 表现 | 时长/曲线 |
| --- | --- | --- |
| 按下（按钮/卡片） | 底色加深 6%、整体缩放 0.98 | 120ms `LinearOutSlowIn` |
| 选中（Tab/筛选） | 图标+文字转 `Primary`，容器用 `PrimaryContainer`，指示条用 `pill` | 200ms |
| 禁用 | 透明度 38%，不响应点击 | 立即 |
| 加载/刷新 | 顶部指示器或骨架，**不用整屏遮罩** | 200ms |
| 展开/收起 | 高度渐变 + 淡入淡出 | 200ms `FastOutSlowIn` |
| 拖动跟手 | 位移实时、浮起阴影加深、`zIndex` 提到最上层 | 跟手；归位 200ms |
| 危险操作（删除） | 文字/按钮用 `ExpenseRed`，必须二次确认 | — |

**规则**：任何点击都必须有视觉反馈（≥色变）；手势操作必须有即时跟随，不得"松手才动"。

---

## 6. 组件规范

| 组件 | 规格 |
| --- | --- |
| **主按钮** | 高 48dp、`pill`、`Primary` 填充、`labelLarge`；一屏最多 1 个 |
| **次按钮** | 高 40dp、`sm` 圆角、透明底 + `Outline` 边、文字 `Primary` |
| **文字按钮** | 无底无边，`labelLarge` + `Primary`；仅用于"全部/调整布局"这类轻操作 |
| **卡片** | `lg` 圆角、`Surface` 底、无描边、`card` 级阴影、内边距 `lg`；**首页可排序区块外框统一等高**（= 内容最高值，内容尺寸不变、框内垂直居中，见 §7） |
| **统计卡** | 高 ≥100dp（**用 `heightIn(min)` 不用固定高**，随系统字号自适应）、语义底色、`titleLarge` 数值 + `labelSmall` 说明 |
| **弹窗** | `xl` 圆角、玻璃面（Thick）、宽 ≤ 屏宽-32dp、标题 `titleMedium`、动作右对齐 |
| **底部导航** | 玻璃面（Regular）+ 顶部 1dp 高光边；图标 24dp、标签 `labelSmall`；选中态用 `Primary` |
| **输入框** | `sm` 圆角、`SurfaceVariant` 底、获得焦点时描边转 `Primary`；错误态描边 `ExpenseRed` + 下方提示 |
| **列表项** | 高 ≥56dp、`md` 圆角、左图标 24dp + 主文本 + 右侧操作；危险操作右滑或长按 |
| **空态** | 图标（`Outline` 色）+ `titleMedium` 标题 + `bodyMedium` 说明，上下留白 `xxl` |

---

## 7. 各页侧重点

| 页面 | 主视觉 | 侧重 | 避免 |
| --- | --- | --- | --- |
| **首页** | 顶部问候 + 生活指数 | **概览**：一眼看到今天的关键数字与待办；玻璃用于语录卡与统计卡 | 区块过多、信息过载（已支持拖拽排序，默认顺序应保持"由概括到细节"） |
| **记账** | 本月收支数字 | **数据**：金额层级清晰（大号 + 红绿语义），列表紧凑 | 装饰性图形干扰读数字 |
| **日程** | 今天待办列表 | **行动**：完成/过期状态一眼可辨；今日项最靠前 | 已完成内容抢占首屏（已完成区默认收起） |
| **健康** | 体重趋势 | **趋势**：图表为主，数值为辅；曲线用 `Tertiary` | 图表与卡片底色对比不足 |
| **书影音** | 封面墙 | **视觉**：以图为主，文字压到最低；封面统一 2:3 比例 | 封面高度不齐导致栅格错位 |
| **设置** | 分组列表 | **克制**：无玻璃、无强调色，仅分隔线与文字层级 | 用主色做行背景 |

---

## 8. 响应式与可读性

1. **字号自适应**：所有卡片/按钮用 `heightIn(min=…)`，禁用 `height(固定)`；系统字号放大到 1.3× 时不得裁切文字。
2. **窄屏（<360dp）**：统计卡由 3 列降为 2 列或纵向堆叠；正文边距保持 `lg` 不缩。
3. **大屏/平板**：内容最大宽度 600dp 居中，避免行长过长；卡片可两列。
4. **横屏**：以滚动为主，不做分栏（个人 App，横屏非主场景）。
5. **深色模式**：当前**不支持**（暖调亮色是产品设定）；如将来支持，玻璃令牌的 alpha 需整体上调一档。

---

## 9. 落地现状（v1.7.0）

| 项 | 状态 |
| --- | --- |
| 令牌层 `ui/theme/Tokens.kt` | ✅ 已建立（间距/圆角/阴影/动效/玻璃三档） |
| 主题 `shapes` 接入 MaterialTheme | ✅ 全局生效（卡片/弹窗/按钮自动合规） |
| 玻璃组件 `ui/components/GlassSurface.kt` | ✅ 已提供（三档强度 + 高光边 + 暖色阴影） |
| 底部导航玻璃化 | ✅ 已应用 |
| 弹窗玻璃化 | ✅ 已应用于共享确认框 |
| 卡片令牌化 | ✅ 统计卡 / 语录卡已应用 |
| 其余页面滚动改造 | ⏳ 后续按 §7 逐页推进 |
