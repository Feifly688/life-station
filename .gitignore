# ── Gradle / 构建产物 ─────────────────────────────────────────
.gradle/
build/

# APK 构建产物（统一由 GitHub Releases 托管，不入源码分支）
*.apk

# ── 签名与密钥（严禁入库，详见 SECURITY.md）─────────────────────
keystore.properties
signing.properties
*.jks
*.keystore
*.p12
*.pfx
*.pem
*.key
*.bks

# ── 环境变量与凭据 ────────────────────────────────────────────
.env
.env.*
secrets.xml
*.secret

# ── 本机相关配置（SDK 路径等）─────────────────────────────────
local.properties

# ── IDE ──────────────────────────────────────────────────────
.idea/
*.iml
captures/

# ── AI 助手工作数据（非项目内容）──────────────────────────────
.workbuddy/

# ── 临时 / 备份文件（可能含个人数据）──────────────────────────
*.tmp
*.bak
*.log

# ── 操作系统 ─────────────────────────────────────────────────
.DS_Store
Thumbs.db

# 代理拦截 git push 时的 API 兜底工具（本机脚本，不入库）
.api_push.py
.tmp_rel/
